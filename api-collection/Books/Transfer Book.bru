meta {
  name: Transfer Book
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/books/transfer
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "bookid": {{bookId}},
    "format": "{{format}}",
    "knowndeviceid": {{knownDevice}}
  }
}

vars:pre-request {
  bookId: 1573
  format: azw3
  knownDevice: 2
}
